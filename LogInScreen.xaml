<Window x:Class="Bank.LogInScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Bank"
        mc:Ignorable="d"
        Title="LogInScreen" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="13*"/>
            <RowDefinition Height="49*"/>
        </Grid.RowDefinitions>
        <Label HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="48">BANK LOGIN</Label>
        <StackPanel Grid.Row="1" Margin="100,20,100,0" VerticalAlignment="Top" HorizontalAlignment="Center">
            <Label FontSize="36">Username</Label>
            <TextBox FontSize="24" x:Name="UserBox"></TextBox>
            <Label FontSize="36">Password</Label>
            <PasswordBox FontSize="24" x:Name="PassBox"></PasswordBox>
            <Button FontSize="36" Margin="10,20,10,0" VerticalAlignment="Center" Click="LogIn_ButtonClick" x:Name="LogInButton">Log-in</Button>
        </StackPanel>
    </Grid>
</Window>
